include ../../pug/vars

if blocked
	.blocked.case
		.blocked__title= blocked.title
		.blocked__wrap.row
			.blocked__img
				img(src=blocked.img alt="")
			.blocked__content
				.blocked__text= blocked.text